// Count number of elements in JavaScript array
// Contar o número de elementos na matriz JavaScript

// Write a function that takes an array (a) as argument
// Escreva uma função que recebe um array (a) como argumento
// Return the number of elements in a
// Retorna o número de elementos em a

function myFunction(a) {
  return a.length;
}

// Test Cases
console.log(myFunction([1, 2, 2, 4])); // 4
console.log(myFunction([9, 9, 9])); // 3
console.log(myFunction([4, 3, 2, 1, 0])); //5
